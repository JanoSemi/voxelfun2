# SCsub

Import("env")

source_files = ["*.cpp", "worldgen/*.cpp", "worldgen/biomes/*.cpp", "entities/*.cpp", "entities/player/*.cpp"]

for f in source_files:
    env.add_source_files(env.modules_sources, f)
